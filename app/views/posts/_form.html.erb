<style>
  p{
      font-family: Archivo, sans-serif;
      font-style: italic;
      font-size: 12px;
  }

</style>
<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-6">

      <%= form_with(model: post, class: "form") do |form| %>
        <% if post.errors.any? %>
          <div class="alert alert-danger">
            <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

            <ul>
              <% post.errors.full_messages.each do |error| %>
                <li><%= error %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="form-group">
          <%= form.label "Titulo do post:", class: "text-center" %>
          <p>Escreva um titulo que atraia! 😉</p>
          <%= form.text_field :title, class: "form-control" %>
        </div>

        <div class="form-group">
          <%= form.label "Conteudo", class: "text-center" %>
          <p>Escreva o que quiser, como quiser! 😎</p>
          <%= form.text_area :content, class: "form-control h-auto", rows: 5 %>
        </div>

        <div class="form-group text-center">
          <%= form.submit "Publicar post!", class: "btn btn-primary" %>
        </div>
      <% end %>

    </div>
  </div>
</div>
